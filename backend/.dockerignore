node_modules
dist
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# All env files (never bake secrets into image)
.env
.env.*

# Docker files (not needed inside the image)
Dockerfile
.dockerignore
docker-compose*.yaml

# Nginx & shell scripts (not part of the app)
nginx.conf
*.sh

# Git
.git
.gitignore

# IDE
.vscode
.idea
*.swp
